import {
  IGroup,
} from '@leafer-ui/interface';

export interface IFreePolylineEditor extends IGroup {

}

export interface IFreePolylineInputEditor {
  color?: string;
  strokeWidth?: number;
  ellipseWidth?: number; 
}